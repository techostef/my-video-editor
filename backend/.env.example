# No API keys required — transcription runs locally via whisper.cpp
# The 'base.en' model (~150 MB) is downloaded automatically on first run.
# To use a larger model, change modelName in lib/whisper.ts:
#   tiny.en | base.en | small.en | medium.en | large
